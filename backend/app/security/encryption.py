class EncryptionService:
    def encrypt_text(self, value: str) -> str:
        # Phase 1 scaffold: wire envelope encryption in a later phase.
        return value

    def decrypt_text(self, value: str) -> str:
        # Phase 1 scaffold: wire envelope encryption in a later phase.
        return value

